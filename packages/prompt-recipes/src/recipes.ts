export const RECIPES = {
  zeroShot: (task: string, constraints: string) => `Objective: ${task}\nLimit: ${constraints}`,
  fewShotPatch: (instruction: string, existing: string) => `User: ${instruction}\nExisting: ${existing}\nEdit: `,
  toolReAct: (tools: string[], goal: string) => `Tools: ${tools.join(', ')}\nTask: ${goal}\nPolicy: Use native tool calling first. For Gemma 4, function calls are handled by runtime; never print tool control tokens or fake tool JSON in normal text. If a tool is needed, issue one native tool call and wait for result. If no tool is needed, answer directly.\nCall: `,
  evaluation: (output: string) => `Check for syntax errors. Output: ${output}\nReturn OK, or REJECT(reason).`,
  fileSummary: (content: string) => `Summarize this file in 2 sentences max. File:\n${content}\nSummary:`,
  codeReview: (diff: string) => `Review this diff. Focus ONLY on logic flaws/bugs. Under 3 lines.\nDiff:\n${diff}\nReview:`,
  baselineInstruction: () => `You are a local coding assistant. Be concise and execution-focused. Prefer native tool calling for file/shell/workspace actions. For Gemma 4 native tools, do not output raw control tokens (<|tool_call|>, <|tool_response|>, <start_function_call>, <start_function_response>) or simulated tool JSON in plain text. Never invent file changes or command outputs.`,
  gemma4SystemPrompt: () => `Gemma 4 native mode: use runtime-native function calling only. Never emit tool control tokens in plain text; runtime will format <|tool_call|> and <|tool_response|> internally. If tool unavailable, explain limitation briefly and continue with best direct answer.`,
  factualityGuard: (text: string) => `Check if this output contains hallucinations or unfounded claims. Text:\n${text}\nVerdict (OK/WARN + reason):`,
  selfCheck: (output: string) => `Does this output follow all constraints and solve the task? Output:\n${output}\nAnswer (YES/NO + improvement):`,
  fileSynthesis: (files: string[]) => `Synthesize a plan to coordinate changes across: ${files.join(', ')}. Outcome:`,
  quickInspect: (task: string) => `Inspect the workspace before answering repo or file questions. If the answer is already in context, answer directly.\nUser request: ${task}`,
  targetedEdit: (task: string) => `Inspect the target files before editing. Use tools for file or shell actions.\nUser request: ${task}`,
  docGeneration: (task: string) => `Inspect manifests, README files, or entry points before writing docs. Use tools for any file changes.\nUser request: ${task}`,
  codeReviewWorkflow: (task: string) => `Inspect the diff or files before reviewing. Prioritize concrete bugs, regressions, and missing tests.\nUser request: ${task}`,
  toolCorrection: () => `Your previous reply simulated tool usage in plain text. Do not simulate tool calls. Use only runtime-native tool API. Never output control tokens (<|tool_call|>, <|tool_response|>, <|tool_calls_begin|>, <start_function_call>, <start_function_response>) or fabricated tool JSON in response body. If no tool needed, answer plainly and do not claim files changed.`,
  manualToolProtocol: (examples: string[]) => `Use this lightweight JSON tool protocol. When you need a tool, reply with EXACTLY one JSON object and nothing else.\nAllowed JSON shapes:\n${examples.join('\n')}\n{"final":"short answer"}\nRules: choose one action at a time, wait for the next message after each tool result, and never invent tool outputs.`,
  manualToolCorrection: () => `Manual fallback active. Reply with exactly one valid JSON object only. Use {"action":"toolName","args":{...}} for one tool step or {"final":"..."} for final answer. Never output raw tool tokens, prose, or markdown fences.`,
  directChatInstruction: () => `Reply directly. Do not invent tools. Keep output low-latency and concise.`,
};
