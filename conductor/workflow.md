# Workflow

Follow `AGENTS.md` first.

- never work on `main`
- preserve `Fix Ollama default runtime and clean sessions`
- inspect current commit patch before edits
- keep normal tests deterministic
- keep real-model smoke opt-in
- separate code status, automated tests, live model proof, and known limits

Commit style:

- terse Conventional Commit subject where possible
- commit meaningful chunks after validation
